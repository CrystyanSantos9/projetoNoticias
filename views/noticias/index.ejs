<%- include('../partials/header') %>

  <div class="starter-template">
    <h1>Notícias Públicas</h1>
    <table>
        <% noticias.forEach( noticia => { %> 
            <tr>
                <td><span class="badge bg-secondary"><%= noticia.title %></span></h1></td>
            </tr>
            <%}) %>
    </table>
  </div>

  <%- include('../partials/footer') %>